services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data/
    networks:
      - proxynet

networks:
  proxynet:
    name: proxynet
    external: true